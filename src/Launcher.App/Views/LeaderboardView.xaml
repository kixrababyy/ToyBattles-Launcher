<UserControl x:Class="Launcher.App.Views.LeaderboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Launcher.App.Views"
             xmlns:vm="clr-namespace:Launcher.App.ViewModels"
             xmlns:conv="clr-namespace:Launcher.App.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Transparent">

    <UserControl.Resources>
        <conv:DdsLevelIconConverter x:Key="DdsLevelIconConverter"/>
    </UserControl.Resources>
             
    <Grid Margin="36,36,36,36">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Top Players -->
        <Border Grid.Column="0" Background="#90080D14" CornerRadius="16" BorderBrush="#2026C0F0" BorderThickness="1" Padding="24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Top 10 Players" FontSize="26" FontWeight="Black" Foreground="#26C0F0" Margin="0,0,0,20" FontFamily="Segoe UI"/>
                
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding TopPlayers}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#40141926" CornerRadius="8" Margin="0,0,0,10" Padding="15,12">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="40"/>
                                            <ColumnDefinition Width="50"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <!-- Rank -->
                                        <TextBlock Grid.Column="0" Text="{Binding Rank}" FontSize="22" FontWeight="Black" VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#707A8A"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Rank}" Value="1">
                                                            <Setter Property="Foreground" Value="#FFD700"/>
                                                            <Setter Property="Effect">
                                                                <Setter.Value>
                                                                    <DropShadowEffect Color="#FFD700" BlurRadius="10" ShadowDepth="0" Opacity="0.5"/>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Rank}" Value="2">
                                                            <Setter Property="Foreground" Value="#C0C0C0"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Rank}" Value="3">
                                                            <Setter Property="Foreground" Value="#CD7F32"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <!-- Level Icon -->
                                        <Image Grid.Column="1" 
                                               Source="{Binding Level, Converter={StaticResource DdsLevelIconConverter}}" 
                                               Width="28" Height="28" 
                                               HorizontalAlignment="Left" VerticalAlignment="Center"
                                               RenderOptions.BitmapScalingMode="HighQuality"/>
                                        <!-- Name -->
                                        <TextBlock Grid.Column="2" Text="{Binding Name}" FontSize="16" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Top Clans -->
        <Border Grid.Column="2" Background="#90080D14" CornerRadius="16" BorderBrush="#20FF4D7D" BorderThickness="1" Padding="24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Top 10 Clans" FontSize="26" FontWeight="Black" Foreground="#FF4D7D" Margin="0,0,0,20" FontFamily="Segoe UI"/>
                
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding TopClans}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#40141926" CornerRadius="8" Margin="0,0,0,10" Padding="15,12">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="40"/>
                                            <ColumnDefinition Width="40"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <!-- Rank -->
                                        <TextBlock Grid.Column="0" Text="{Binding Rank}" FontSize="22" FontWeight="Black" VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#707A8A"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Rank}" Value="1">
                                                            <Setter Property="Foreground" Value="#FFD700"/>
                                                            <Setter Property="Effect">
                                                                <Setter.Value>
                                                                    <DropShadowEffect Color="#FFD700" BlurRadius="10" ShadowDepth="0" Opacity="0.5"/>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Rank}" Value="2">
                                                            <Setter Property="Foreground" Value="#C0C0C0"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Rank}" Value="3">
                                                            <Setter Property="Foreground" Value="#CD7F32"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <!-- Clan Icon Placeholder -->
                                        <TextBlock Grid.Column="1" Text="&#xEA3A;" FontFamily="Segoe MDL2 Assets" FontSize="18" Foreground="#2ECC71" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                        <!-- Name -->
                                        <TextBlock Grid.Column="2" Text="{Binding Name}" FontSize="16" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
